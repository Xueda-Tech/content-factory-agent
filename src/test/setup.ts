/**
 * Vitest setup file.
 *
 * Loads custom jest-dom matchers so tests can use assertions like
 * `expect(el).toBeInTheDocument()` without importing them per-file.
 */
import "@testing-library/jest-dom/vitest";
